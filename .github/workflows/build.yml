name: Build & Publish

on:
    push:
        branches:
            - main

jobs:
    build:
        name: Build & Publish
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Install packages
              run: npm install
            - name: Build
              run: npm run build